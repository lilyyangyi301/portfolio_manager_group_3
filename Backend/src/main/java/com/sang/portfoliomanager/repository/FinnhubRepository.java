package com.sang.portfoliomanager.repository;

import com.sang.portfoliomanager.entity.Finnhub;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FinnhubRepository extends JpaRepository<Finnhub, Long> {
}
